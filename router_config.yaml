# Deprecated (compat): moved to `config/router_config.yaml`
#
# Keep this file to avoid breaking old links/integrations that expect `router_config.yaml` at repo root.
# Consumers should read `config/router_config.yaml` as the SSOT.
include: "config/router_config.yaml"



